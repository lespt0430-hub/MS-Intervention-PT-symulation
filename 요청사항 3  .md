너는 '수석 3D 웹 엔지니어'야. 
첨부된 4장의 도면 이미지를 참고하여 [광주보건대학교 부속 물리치료실] 3D 공간을 React Three Fiber(R3F) 모듈형 구조로 구현해 줘.

[이미지 역할 구분]
1. '물리치료실 전체 도면.png': 전체 3D 메인 레이아웃 및 구역 배치 기준
2. '운동치료실 내부도면.png': 좌측 [운동치료실] 내부 가구/장비 배치 기준
3. '전기치료실 내부도면.png': 중앙 [전기치료실] 커튼 파티션 및 의료기기 카트 배치 기준
4. '도수치료실 내부도면.png': 우측 [도수치료실] 4개 프라이빗 룸 세부 배치 기준
너는 지금부터 '수석 3D 시뮬레이션 엔지니어'이자 '공간 설계자'야. 
나의 로컬 환경(Filesystem, Terminal)에 접근하여 첨부된 'download-file.png' 이미지와 동일한 구조의 [광주보건대학교 부속 물리치료실] 3D 시뮬레이션을 구축하는 것이 목표야.

기술 스택은 React, Three.js (React Three Fiber - R3F), Vite를 사용할 거야.
우리는 이 거대한 작업을 쪼개서 단계별로 진행할 거야. 내가 다음 단계를 지시하기 전까지는 현재 단계의 작업만 수행하고 대기해.

[MCP 도구 자동 실행 규칙]
1. 파일 생성/수정 시 반드시 `Filesystem MCP`를 사용해 로컬 프로젝트 폴더에 직접 저장해.
2. 패키지 설치 및 서버 실행은 `Terminal MCP`를 이용해 직접 수행해.
3. 코드를 작성하거나 수정한 뒤에는 반드시 `Puppeteer MCP`로 'http://localhost:5173'에 접속하여 화면 스크린샷을 캡처해.
4. 첨부된 3D 화면을 내가 제공한 도면 이미지와 직접 비교해 보고, 벽면 위치나 카메라 앵글이 어색하다면 스스로 코드를 수정한 뒤 다시 캡처해서 검증하는 과정을 거쳐.

[1단계 실행 지시]
위 MCP 규칙을 준수하여 Vite + R3F 프로젝트를 생성하고, 전체 바닥판과 외벽, 그리고 3개 구역 구분 벽면을 세운 뒤 Puppeteer 캡처 이미지와 함께 1단계 완료 보고를 해줘.
[구역별 모듈화 설계 요구사항]
- src/components/ExerciseRoom.tsx : 천장 슬링 트랙(Sling Track), 보행용 평행봉, 매트 베드, 필라테스 리포머, 짐볼 랙
- src/components/ElectroRoom.tsx : 중앙 복도 양옆으로 배치된 커튼 파티션 침대, 이동식 전기치료기 카트
- src/components/ManualRoom.tsx : 총 4개의 독립된 방, 도수치료 전용 베드, 수납장, 탁상 조명, 옷장
- src/components/MainLayout.tsx : 외벽, 출입문 간판("광주보건대학교 부속 물리치료실"), 3개 구역 통합

---
[현재 진행 지시사항 : 1단계]
1. Terminal을 활용해 Vite + React(TS) 프로젝트(`pt-clinic-3d`)를 생성하고 `three`, `@react-three/fiber`, `@react-three/drei`를 설치해 줘.
2. `MainLayout.tsx`를 생성해 전체 바닥판과 외벽, 그리고 3개 구역을 나누는 구분 벽면을 먼저 세워줘.
3. 각 구역 위치에 빈 컴포넌트(`ExerciseRoom`, `ElectroRoom`, `ManualRoom`)를 배치해 구조만 잡아줘.
4. OrbitControls와 기본 조명을 추가한 뒤 `npm run dev`로 실행하여 에러가 없는지 검증해 줘.

완료되면 "1단계: 메인 공간 구조 및 3개 구역 컴포넌트 분리 완료"라고 출력하고 2단계를 기다려.